# TODO

## Refresh Token 全部设备退出与孤立索引清理

### 目标

基于已经实现的 refresh token 用户反向索引，增加 `logout-all`，并清理 refresh key 自然过期后留下的孤立索引成员。

### Redis 结构

```text
vsdu:auth:rt:{refreshTokenHash} -> userId
vsdu:auth:rt-set:{userId} -> Set<refreshTokenHash>
```

### 待实现流程

- 全部设备退出：读取该用户的全部 refresh token hash，批量删除对应 refresh key，最后删除用户反向索引。
- 孤立成员清理：在 `logout-all` 或定时任务中移除已经没有对应 refresh key 的 hash。

### 一致性要求

- 使用 Redis Lua 脚本或等效原子操作，确保批量删除 refresh key 与删除用户反向索引同步完成。
- 与 refresh token 轮换并发执行时，不能留下仍然有效但未被 `logout-all` 删除的 token。
- 避免使用 `KEYS` 或扫描全站 refresh token 实现 `logout-all`。

### 验收条件

- `logout-all` 后，该用户此前签发的所有 refresh token 均不可再使用。
- `logout-all` 不影响其他用户的 refresh token。
- 过期 refresh token 对应的孤立索引成员能够被安全清理。

## 搜索建议的媒体与标签层级状态过滤

### 目标

确保 `/search/suggestions` 返回的所有建议都遵守城市、校区、地点和媒体的完整启用状态约束。

### 当前问题

- `LOCATION` 建议已经同时过滤停用校区和停用城市，`CAMPUS` 建议也已经过滤停用城市。
- `MEDIA` 和 `TAG` 建议仍从只校验媒体自身 `status` 的查询生成，可能包含停用地点、校区或城市下的媒体及其标签。

### 待处理

- 为媒体建议查询补齐地点、校区和城市的启用状态过滤。
- 标签建议只从通过完整层级状态过滤的媒体中提取。
- 尽量复用媒体综合搜索的可见性规则，避免两套规则再次分叉。

### 验收条件

- 停用地点、校区或城市下的媒体不会产生 `MEDIA` 或 `TAG` 建议。
- `LOCATION`、`CAMPUS`、`MEDIA` 和 `TAG` 四类建议使用一致的层级可见性规则。

## 发现首页话题是否按城市筛选

### 目标

确认 `/discovery/home` 传入 `cityId` 时，`topics` 及其 `mediaCount` 应返回城市范围数据还是全局数据。

### 当前问题

发现首页的精选媒体、最新媒体、热门标签和校区分区会按 `cityId` 筛选，但 `topics` 直接调用全局话题列表，话题中的 `mediaCount` 也不带城市条件。

### 待处理

- 若产品要求城市隔离，为话题列表和媒体计数增加城市筛选。
- 若产品要求展示全局话题，在接口文档和客户端交互中明确该语义。

### 验收条件

- 产品语义得到确认，代码行为、接口文档和客户端展示保持一致。

## 收藏夹相关接口的用户资格规则统一

### 目标

统一单媒体收藏、收藏夹管理和批量收藏接口的用户资格规则。

### 当前问题

`/media/{mediaId}/favorites` 会校验统一认证正式正常用户，而 `/favorite-folders/**` 和 `/favorites/batch` 只要求用户已登录。

### 待处理

- 确认收藏夹管理和批量收藏是否也必须限制为统一认证正式正常用户。
- 将资格校验集中到可复用逻辑，避免收藏入口之间继续分叉。

### 验收条件

- 同一用户通过不同收藏入口时适用相同的资格规则和错误码。

## 批量 REMOVE 的 folderId 语义统一

### 目标

统一 `BatchFavoriteRequest.folderId` 的注释、接口文档与服务实现。

### 当前问题

DTO 注释称 `REMOVE` 时 `folderId` 表示源收藏夹，但服务实现忽略该字段，并按媒体 ID 删除当前用户的全部对应收藏关系。

### 待处理

- 确认 `REMOVE` 应只从指定收藏夹移除，还是全局取消媒体收藏。
- 根据确认结果修改 DTO、校验规则、服务实现和接口文档。

### 验收条件

- `folderId` 是否必填、实际删除范围和文档描述完全一致。

## 收藏夹 clearCover 语义确认

### 目标

明确 `clearCover=true` 表示清除手动封面，还是要求响应中的封面完全为空。

### 当前问题

当前实现只清除手动配置的 `coverMediaId`；响应转换时仍会回退到收藏夹中最近收藏的可见媒体，因此响应中的 `coverMediaId` 和 `coverUrl` 可能继续非空。

### 待处理

- 若保留自动回退封面，将字段说明和客户端文案改为“清除手动封面”。
- 若要求完全清空，增加能够禁用自动回退封面的状态或调整响应逻辑。

### 验收条件

- 操作名称、请求字段说明和响应结果不会让客户端误判封面已经为空。

## media_favorite 唯一索引核验

### 目标

确认部署数据库存在 `media_favorite(user_id, media_id)` 唯一索引，并将该约束纳入可追踪的数据库结构管理。

### 当前问题

收藏 Mapper 和收藏计数逻辑依赖该唯一索引，但仓库中没有数据库迁移文件可供核验。

### 待处理

- 核验开发、测试和生产数据库中的实际索引。
- 在仓库采用的数据库迁移或建表方案中补充唯一约束。

### 验收条件

- 重复收藏无法产生多条相同用户、相同媒体的收藏关系。
- 新环境初始化后会自动具备该唯一索引。

## 搜索关键字 LIKE 通配符语义确认

### 目标

确认搜索关键字中的 `%` 和 `_` 应按 SQL `LIKE` 通配符处理，还是按普通字符进行字面匹配。

### 当前问题

当前参数绑定能够避免 SQL 注入，但 `%` 和 `_` 会保留通配符语义，可能返回超出用户字面输入预期的结果。

### 待处理

- 若要求字面匹配，在媒体、地点和校区相关搜索中统一转义 `%`、`_` 及转义字符本身，并显式声明 `ESCAPE` 字符。
- 若保留通配符能力，在接口文档和前端交互中明确说明。

### 验收条件

- `%`、`_` 和转义字符的行为有自动化测试覆盖。
- 搜索建议与媒体综合搜索采用一致的关键字匹配语义。
