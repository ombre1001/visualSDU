<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.sduonline.business.mapper.MediaSearchMapper">

    <sql id="joins">
        FROM media m
        INNER JOIN location l ON l.id = m.location_id AND l.status = 1
        INNER JOIN campus c ON c.id = l.campus_id AND c.status = 1
        INNER JOIN city ci ON ci.id = c.city_id AND ci.status = 1
    </sql>

    <sql id="conditions">
        WHERE m.status = 1
        <if test="query.q != null and query.q != ''">
            AND (
                m.title LIKE CONCAT('%', #{query.q}, '%')
                OR m.description LIKE CONCAT('%', #{query.q}, '%')
                OR m.tags LIKE CONCAT('%', #{query.q}, '%')
                OR l.name LIKE CONCAT('%', #{query.q}, '%')
                OR l.address LIKE CONCAT('%', #{query.q}, '%')
                OR c.name LIKE CONCAT('%', #{query.q}, '%')
                OR ci.name LIKE CONCAT('%', #{query.q}, '%')
            )
        </if>
        <if test="query.cityId != null">
            AND ci.id = #{query.cityId}
        </if>
        <if test="query.campusId != null">
            AND c.id = #{query.campusId}
        </if>
        <if test="query.locationId != null">
            AND l.id = #{query.locationId}
        </if>
        <if test="query.tag != null and query.tag != ''">
            AND LOCATE(
                CONCAT('|', #{query.tag}, '|'),
                CONCAT('|', COALESCE(m.tags, ''), '|')
            ) > 0
        </if>
        <if test="query.shotYear != null">
            AND YEAR(m.shot_at) = #{query.shotYear}
        </if>
        <if test="query.topicId != null">
            AND EXISTS (
                SELECT 1
                FROM topic_media tm
                INNER JOIN topic t ON t.id = tm.topic_id AND t.status = 1
                WHERE tm.media_id = m.id
                  AND tm.topic_id = #{query.topicId}
            )
        </if>
    </sql>

    <select id="countSearchMedia" resultType="long">
        SELECT COUNT(*)
        <include refid="joins"/>
        <include refid="conditions"/>
    </select>

    <select id="searchMedia"
            resultType="cn.sduonline.business.data.projection.MediaSummaryRow">
        SELECT
            m.id,
            m.title,
            m.location_id,
            l.name AS location_name,
            COALESCE(NULLIF(m.thumbnail_key, ''), m.object_key) AS thumbnail_key,
            m.tags,
            m.shot_at,
            m.view_count,
            m.like_count,
            m.favorite_count
        <include refid="joins"/>
        <include refid="conditions"/>
        ORDER BY
        <choose>
            <when test='sort == "newest"'>
                COALESCE(m.shot_at, m.created_at) DESC, m.id DESC
            </when>
            <when test='sort == "oldest"'>
                COALESCE(m.shot_at, m.created_at) ASC, m.id ASC
            </when>
            <when test='sort == "hot"'>
                (
                    COALESCE(m.favorite_count, 0) * 5
                    + COALESCE(m.like_count, 0) * 3
                    + COALESCE(m.view_count, 0)
                ) DESC,
                m.id DESC
            </when>
            <otherwise>
                CASE
                    WHEN m.title = #{query.q} THEN 100
                    WHEN l.name = #{query.q} THEN 95
                    WHEN c.name = #{query.q} THEN 90
                    WHEN ci.name = #{query.q} THEN 85
                    WHEN m.title LIKE CONCAT(#{query.q}, '%') THEN 80
                    WHEN m.tags LIKE CONCAT('%', #{query.q}, '%') THEN 60
                    WHEN m.description LIKE CONCAT('%', #{query.q}, '%') THEN 40
                    ELSE 10
                END DESC,
                (
                    COALESCE(m.favorite_count, 0) * 5
                    + COALESCE(m.like_count, 0) * 3
                    + COALESCE(m.view_count, 0)
                ) DESC,
                m.id DESC
            </otherwise>
        </choose>
        LIMIT #{offset}, #{limit}
    </select>

</mapper>
