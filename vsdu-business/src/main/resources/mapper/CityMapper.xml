<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.sduonline.business.mapper.CityMapper">

    <select id="selectEnabledSummaries"
            resultType="cn.sduonline.business.data.projection.CitySummaryRow">
        SELECT
            c.id,
            c.name,
            c.code,
            c.province,
            c.cover_key,
            c.description,
            (
                SELECT COUNT(*)
                FROM campus cp
                WHERE cp.city_id = c.id
                  AND cp.status = 1
            ) AS campus_count
        FROM city c
        WHERE c.status = 1
        ORDER BY c.sort_order, c.id
    </select>

</mapper>
